[bumpversion]
current_version = "1.0.4"
commit = True
tag = True
parse = "(_*version_*\s+=\s+)?(?P<major>\d+)\.(?P<minor>\d+)\.(?P<patch>\d+)(\-(?P<release>[a-z]+))?"
serialize = 
	"{major}.{minor}.{patch}-{release}"
	"{major}.{minor}.{patch}"

[bumpversion:file:setup.py]

